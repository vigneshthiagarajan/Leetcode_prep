# Write your MySQL query statement below
select seller_id from (select seller_id, sum(price) as sum_price from sales group by seller_id having sum_price = (select max(total) as max_total from (select seller_id, sum(price) as total from sales group by seller_id) t1))t2;