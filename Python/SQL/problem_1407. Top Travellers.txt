# Write your MySQL query statement below
SELECT NAME, SUM(COALESCE(DISTANCE,0)) AS TRAVELLED_DISTANCE FROM USERS U LEFT OUTER JOIN RIDES R ON U.ID = R.USER_ID GROUP BY R.USER_ID ORDER BY TRAVELLED_DISTANCE DESC, NAME ASC;